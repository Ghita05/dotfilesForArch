diff --git a/modules/bar/clock/Weather.qml b/modules/bar/clock/Weather.qml
index 0043e33..9cef782 100644
--- a/modules/bar/clock/Weather.qml
+++ b/modules/bar/clock/Weather.qml
@@ -152,8 +152,12 @@ StyledRect {
     function updateWeather() {
         var location = Config.weather.location.trim();
         if (location.length === 0) {
-            geoipProcess.command = ["curl", "-s", "https://ipapi.co/json/"];
-            geoipProcess.running = true;
+            // ipapi.co geoIP fallback removed: it returns an HTTP 403
+            // Cloudflare bot-challenge page on this network (confirmed by
+            // direct curl, not JSON), so it never worked here and silently
+            // masked the real problem. No client-side geoIP replaces it —
+            // set a location (city name or "lat,lon") in Settings instead.
+            weatherContainer.weatherVisible = false;
             return;
         }
 
@@ -187,39 +191,6 @@ StyledRect {
         scheduleNextDayUpdate();
     }
 
-    Process {
-        id: geoipProcess
-        running: false
-        command: []
-
-        stdout: StdioCollector {
-            waitForEnd: true
-            onStreamFinished: {
-                var raw = text.trim();
-                if (raw.length > 0) {
-                    try {
-                        var data = JSON.parse(raw);
-                        if (data.latitude && data.longitude) {
-                            cachedLat = data.latitude.toString();
-                            cachedLon = data.longitude.toString();
-                            fetchWeatherWithCoords(cachedLat, cachedLon);
-                        } else {
-                            weatherContainer.weatherVisible = false;
-                        }
-                    } catch (e) {
-                        weatherContainer.weatherVisible = false;
-                    }
-                }
-            }
-        }
-
-        onExited: function (code) {
-            if (code !== 0) {
-                weatherContainer.weatherVisible = false;
-            }
-        }
-    }
-
     Process {
         id: geocodingProcess
         running: false
